apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/tailnet-ip: '100.79.70.119'
  name: cloud-mariadb-egress
spec:
  externalName: cloud-mariadb-egress
  type: ExternalName


# ---
# apiVersion: v1
# kind: Service
# metadata:
#   labels:
#     app: mariadb
#   annotations:
#     tailscale.com/expose: 'true'
#   name: mariadb-access
#   namespace: mariadb
# spec:
#   type: ClusterIP
#   ports:
#   - port: 3306
#     protocol: TCP
#     targetPort: 3306
#     name: web
#   selector:
#     app: mariadb