apiVersion: v1
kind: Service
metadata:
  name: headlamp
  labels:
    tailscale.com/proxy-class: "enable-tun"
  annotations:
    tailscale.com/expose: 'true'
    tailscale.com/hostname: 'cloud-headlamp'